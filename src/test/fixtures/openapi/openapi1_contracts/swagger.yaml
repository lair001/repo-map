swagger: "2.0"
info:
  title: Fixture Swagger YAML API
  version: "2.0"
host: api.example.invalid
basePath: /v2
schemes:
  - https
paths:
  /reports:
    get:
      operationId: listReports
      responses:
        "200":
          description: ok
          schema:
            $ref: "#/definitions/Report"
definitions:
  Report:
    type: object
securityDefinitions:
  ApiKeyAuth:
    type: apiKey
    in: query
    name: api_key
