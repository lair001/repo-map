openapi: 3.0.0
info:
  title: Example API
  version: 1.0.0
paths:
  /pets:
    get:
      responses:
        '200':
          $ref: '#/components/responses/Pets'
components:
  responses:
    Pets:
      description: ok
  schemas:
    Remote:
      $ref: https://example.invalid/openapi.yaml#/components/schemas/Remote
