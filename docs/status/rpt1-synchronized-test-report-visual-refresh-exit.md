# RPT1 Synchronized Test Report Visual Refresh Exit

Status: Complete

## Scope

RPT1 refreshed the nix-darwin generated HTML test report visual system and added
a comparable static HTML report path to RepoMap's test runner with
`tools/run_tests.py --report`.

The slice stayed in report-generator and report-status scope:

- no RepoMap graph extraction behavior changed;
- no canonicalization changed;
- no storage schema changed;
- no public graph readback defaults changed;
- no MCP behavior changed;
- no CSS or JavaScript extractor was added;
- no external assets were executed or fetched;
- no third-party frontend dependencies were added.

## Mockups Inspected

The prompt paths were top-level shorthand; the actual mockup files inspected
were:

- `/Users/slair/.flakes/.superpowers/brainstorm/22564-1782302060/content/test-report-tree-mockup.html`
- `/Users/slair/.flakes/.superpowers/brainstorm/22564-1782302060/content/drilldown-pages-mockup.html`
- `/Users/slair/.flakes/.superpowers/brainstorm/22564-1782302060/content/drilldown-pages-mockup-v2.html`

The adopted visual direction came from those mockups:

- dark terminal/dashboard background;
- card-like report sections with subtle borders;
- compact monospace rows;
- colored status badges;
- stronger report-header hierarchy;
- constrained grid columns with `minmax(0, 1fr)`;
- ellipsized long paths;
- horizontally scrollable code and output blocks.

## Reports Inspected

The existing generated report output under
`/Users/slair/.flakes/nix-darwin/.test-reports` was inspected, including:

- `unit/latest/index.html`
- `unit/latest/static/report.css`
- `unit/latest/coverage/*.html`
- `unit/latest/tests/*.html`
- `all/latest/index.html` after regeneration

`.test-reports` remains generated and ignored by git. It was regenerated for
verification but not committed.

## Repositories Changed

### `.flakes`

Changed the report source under `/Users/slair/.flakes/nix-darwin`:

- `tools/test/report/static/report.css`
- `tools/test/report/templates/index.html`
- `tools/test/report/pages.py`
- `src/test/unit/python/test_report_pages.unit.test.py`

The CSS is the shared source for nix-darwin generated reports. The renderer
copies it into each `.test-reports/<scope>/latest/static/report.css` directory
when a report is generated.

### `repo-map`

Added a lightweight stdlib HTML report renderer for RepoMap:

- `tools/run_tests.py`
- `tools/test_report.py`
- `tools/test/report/static/report.css`
- `tools/test/report/static/report.js`
- `src/test/unit/python/repomap_kg/test_test_report.unit.test.py`
- `README.md`
- `.gitignore`

RepoMap reports are generated by adding `--report` to the existing runner:

```sh
python3 tools/run_tests.py --suite unit --report
```

The report is written to `.test-reports/<suite>/latest`.

## Synchronization Model

The report assets are intentionally synchronized but not packaged as one shared
library yet:

- nix-darwin source CSS:
  `/Users/slair/.flakes/nix-darwin/tools/test/report/static/report.css`
- nix-darwin source JS:
  `/Users/slair/.flakes/nix-darwin/tools/test/report/static/report.js`
- RepoMap source CSS:
  `tools/test/report/static/report.css`
- RepoMap source JS:
  `tools/test/report/static/report.js`

Until a shared package is worth the extra plumbing, report visual changes should
update both copies in the same slice and keep this synchronization boundary
explicit.

## Visual Changes

RPT1 changed the generated report visual language to:

- dark default theme via `color-scheme: dark`;
- dashboard-style header panel;
- summary badges for test totals and coverage gates;
- darker report section surfaces;
- status colors aligned with pass, warn/skipped, and fail states;
- compact monospace report typography;
- row hover states;
- better path and source-cell overflow handling;
- responsive horizontal scrolling for dense grids on narrower screens.

## CSS And JS

CSS changes were concentrated in `report.css`.

The existing `report.js` remained minimal and offline-safe. It still only
preserves tree expansion state in `sessionStorage` and updates
`aria-expanded` on `<summary>` elements.

## Accessibility

RPT1 preserved:

- semantic headings;
- existing `<details>` / `<summary>` keyboard behavior;
- `aria-expanded` updates for tree toggles;
- visible focus styling on tree summaries;
- high-contrast dark foreground/background colors;
- link states that remain usable without network access.

Dense grids use fixed metric/status columns with flexible path/source columns,
and long path/code/output content is constrained inside the relevant cell or
scrollable block.

## Regeneration

To regenerate nix-darwin reports:

```sh
/Users/slair/.flakes/nix-darwin/bin/nix-unit-test --report
/Users/slair/.flakes/nix-darwin/bin/nix-test --report
```

The full host-safe report is written to:

```text
/Users/slair/.flakes/nix-darwin/.test-reports/all/latest
```

To generate a RepoMap report:

```sh
python3 tools/run_tests.py --suite unit --report
```

Any existing suite value can be used, but RPT1 verification focused on
report-generator tests and avoided Postgres-backed integration work.

## Known Gaps

- Report CSS/JS is copied between nix-darwin and RepoMap rather than extracted
  into a package shared by both repos.
- The generated reports were smoke-checked through HTML/CSS hooks and test
  runner output, not browser screenshot automation.
- Existing nix-darwin runner output still prints a known shell warning about
  ignored null bytes during report generation; the report lane passes.

## Verification

Commands run:

```sh
PYTHONPYCACHEPREFIX=/private/tmp/rpt1-pycache python3 -m py_compile /Users/slair/.flakes/nix-darwin/tools/test/report/pages.py /Users/slair/.flakes/nix-darwin/src/test/unit/python/test_report_pages.unit.test.py
/Users/slair/.flakes/nix-darwin/bin/nix-unit-test --report
/Users/slair/.flakes/nix-darwin/bin/nix-test --report
```

`nix-unit-test --report` passed and generated
`/Users/slair/.flakes/nix-darwin/.test-reports/unit/latest`.

`nix-test --report` passed and generated
`/Users/slair/.flakes/nix-darwin/.test-reports/all/latest` with 391/391 tests
passing and all hard coverage gates passing.

After the RepoMap scope correction, final RepoMap verification intentionally
focused on report-generator tests and diff checks rather than Postgres-backed
test suites. Final commands are recorded in the RPT1 commit messages.
